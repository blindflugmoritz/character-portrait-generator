# Anthropic API Configuration (no longer used - moved to Django backend)
# Get your API key from: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=your_api_key_here

# Django Backend API URL
# For local development: http://localhost:8000
# For production: your deployed Django backend URL
PUBLIC_API_URL=https://blindflugstudios.pythonanywhere.com

# Static files base path
# For local development: empty (SvelteKit serves from /static)
# For production on Django: /static (set in .env.production)
# VITE_STATIC_BASE_PATH= (commented out - let .env.production set it)
